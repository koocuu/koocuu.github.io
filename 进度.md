# 进度 · cu-site

> 追加式记录；多 agent 并行时不要覆盖他人条目。开工前先读本文件与 `产品文档.md`。

## 2026-08-17 · About「深耕方向」词序改为 RAG / MCP / Agent / AI 应用开发

**改动与原因**：用户指定四项为「RAG、MCP、Agent 和 AI 应用开发」。中英同步。

**影响范围**：`src/data/i18n.ts`。

**验证**：`npm run build`。

**后续**：无。

## 2026-08-17 · About「深耕方向」加上 AI 应用

**改动与原因**：用户改为四项：RAG、MCP、AI 应用和 Agent 开发。中英同步。

**影响范围**：`src/data/i18n.ts`。

**验证**：`npm run build`。

**后续**：无。

## 2026-08-17 · About「深耕方向」收成 RAG / MCP / Agent 开发

**改动与原因**：用户点选该卡并指定这三项。原先混在全栈/Web/后端长列表里看不清，改为以 RAG、MCP、Agent 开发为主句。中英同步。

**影响范围**：`src/data/i18n.ts`。

**验证**：`npm run build`。About 该卡正文应以这三项起头。

**后续**：无。

## 2026-08-17 · 足迹统计：数字和标签都在竖线左边

**改动与原因**：竖线是整块的右边框，不是夹在标签和数字中间。每项数字在上、标签在下，右对齐贴着竖线。

**影响范围**：`src/pages/[lang]/travel.astro`、`src/styles/global.css`。

**验证**：`npm run build`。桌面应为 `6 / 个国家与地区` 整组在竖线左侧。

**后续**：无。

## 2026-08-17 · 足迹统计数字改到竖线左边

**改动与原因**：hero 右侧统计当前是「标签 | 竖线 | 数字」。按反馈改为「数字 | 竖线 | 标签」，数字右对齐贴线。

**影响范围**：`src/styles/global.css`。

**验证**：`npm run build`。足迹页右侧应为 6 | 个国家与地区。

**后续**：无。

## 2026-08-17 · 修正主页 #projects / #recent 锚定被顶栏挡住

**改动与原因**：点「看我做的东西」会滚到 `#projects`，但 sticky 顶栏约 74px，原来 `scroll-padding-top` 和 `section[id]` 的 `scroll-margin-top` 各 24px，在 Chromium 里叠加后区块顶只离视口 48px，「精选作品」眉题被挡住。改为只在 `html` 上设 `--scroll-offset: 6rem`。

**影响范围**：`src/styles/global.css`。

**验证**：`npm run build`；预览点 CTA 后 `#projects` 眉题应完整露在顶栏下方。

**后续**：无。

## 2026-08-17 · 去掉 text-link 箭头下的单独下划线

**改动与原因**：案例页/此刻页把 `.text-link` 设成 `inline-flex` 后，下划线会分别画在文字和 `→` 上。改回 `inline-block`，箭头作为 `inline-block` 伪元素不再被划线。

**影响范围**：`src/styles/global.css`。

**验证**：`npm run build`。案例页/此刻页「返回首页」箭头下不应再有短线。

**后续**：无。

## 2026-08-17 · Now 页加返回首页

**改动与原因**：此刻已不在一级导航，进来后只剩点品牌 Cu 能回。按案例页/文章页同样的 `text-link` 在标题上加「返回首页」。

**影响范围**：`src/pages/[lang]/now.astro`、`src/data/i18n.ts`、`src/styles/global.css`。

**验证**：`npm run build`。`/zh/now/` 标题上方应有返回首页，点回主页。

**后续**：无。

## 2026-08-17 · 主页「读最新文章」锚定到下方列表

**改动与原因**：该按钮原先跳转 Blog 页；主页已有最近三篇，「查看全部文章」才该去 Blog。改为 `#recent`，与「看我做的东西」一样页内锚定。

**影响范围**：`src/pages/[lang]/index.astro`。

**验证**：`npm run build`。点「读最新文章」应滚到最近写下，不离开主页。

**后续**：无。

## 2026-08-17 · 撤回 About 头像裁切改动

**改动与原因**：主页重构时把圆形头像改成 160/88px 并放大侧脸，看起来奇怪。样式恢复为改版前：桌面 172px、`object-position: center 38%`、无 scale；手机 96px。

**影响范围**：`src/styles/global.css`。

**验证**：`npm run build`。硬刷新关于页，头像应回到原来的圆形裁切。

**后续**：无。

## 2026-08-17 · 修复足迹概览竖线只画第一行

**改动与原因**：`grid-row: 1 / -1` 不能跨隐式行，竖线只出现在第一行。改为明确三行并把每项钉到对应行，竖线 `1 / 4` 拉满。

**影响范围**：`src/styles/global.css`。

**验证**：`npm run build`。硬刷新后三条数字旁应是一条贯通竖线。

**后续**：无。

## 2026-08-17 · About 深耕方向补 RAG / Agent / MCP

**改动与原因**：用户要求「深耕方向」卡加上 RAG、Agent 开发、MCP 开发。中英同步。

**影响范围**：`src/data/i18n.ts`。

**验证**：`npm run build`。

**后续**：无。

## 2026-08-17 · About 能力卡：性能 → 性能优化

**改动与原因**：用户指定「开发者底色」文案把「性能」改成「性能优化」。英文同步为 performance optimization。

**影响范围**：`src/data/i18n.ts`。

**验证**：改文案；`npm run build` 更新 dist。

**后续**：无。

## 2026-08-17 · 足迹概览：标签在竖线左、数字在右

**改动与原因**：用户希望文字在竖线左边。概览改为「标签 | 数字」，一条竖线贯穿三行。

**影响范围**：`src/pages/[lang]/travel.astro`、`src/styles/global.css`、`进度.md`。

**验证**：`npm run build`。宽屏应看到「个国家与地区 / 座城市 / 最早一条记录」在竖线左，数字在右。

**后续**：无。

## 2026-08-17 · 足迹页标题右侧放入概览数字

**改动与原因**：时间轴视图标题和列表都偏左，宽屏右侧空一截。把原来页底的国家/城市/最早记录挪到 hero 右侧（与主页 Now 同一处理），底部统计条删除。年份列从 190px 收到 140px，给条目多一点宽度。

**影响范围**：`src/pages/[lang]/travel.astro`、`src/styles/global.css`、`产品文档.md`。

**验证**：`npm run build`。宽屏足迹页标题右侧应看到三个数字；页底不再有独立统计条。

**后续**：无。

## 2026-08-17 · Blog 页去掉公众号，只留联系页

**改动与原因**：公众号已在联系页；Blog 再放一遍职责重复。Blog 只保留文章列表。

**影响范围**：`src/pages/[lang]/blog/index.astro`、`src/data/i18n.ts`（删 `blog.channels*`）、`产品文档.md`。

**验证**：`npm run build`。Blog 页底部不应再有「棱角计划 / 碳基灵感收容所」；联系页仍有。

**后续**：无。

## 2026-08-17 · 导航去掉「此刻」Tab，保留 `/now/` 页

**改动与原因**：主页 hero 右侧已承担此刻入口，一级导航再放「此刻」重复。导航改为：足迹 / 关于 / 博客 / 联系。`/zh/now/`、`/en/now/` 仍在，首页那一行继续链过去；控制台同步链路不动。

**影响范围**：`src/components/SiteNav.astro`、`src/data/i18n.ts`（删 `nav.now`）、`产品文档.md`。

**验证**：`npm run build` 通过。`dist` 已更新；硬刷新 preview 后顶栏应为 足迹 / 关于 / 博客 / 联系。主页右侧「此刻 · 2026 夏」仍是进入 `/now/` 的入口，不是导航 Tab。

**后续**：无。

## 2026-08-17 · Hero 右侧改放 Now；公众号同步到联系页

**改动与原因**：删照片后 hero 右侧像缺了一列。不加 logo（顶栏已有 Cu，再放会像填洞）。把此刻一行挪进 hero 右侧，左边是观点、右边是正在发生的事。公众号按用户要求也放到联系页；Blog 底部保留。抽了 `WritingChannels.astro` 两页共用。

**影响范围**：`src/pages/[lang]/index.astro`、`src/pages/[lang]/contact.astro`、`src/pages/[lang]/blog/index.astro`、`src/components/WritingChannels.astro`、`src/data/i18n.ts`、`src/styles/global.css`、`产品文档.md`。

**验证**：`npm run build` 通过。preview 1280 宽下 `.hero` 为两列（约 645px + 387px），`.hero-now` 在右侧。`/zh/contact/` 无障碍树含「棱角计划」「碳基灵感收容所」。

**后续**：桌面窄于 820px 时 Now 落到标题下方。若仍觉得右侧偏轻，再考虑换更正面的小头像（会打破「人像只在 About」）。

## 2026-08-16 15:30 · 主页重构落地（观点 → Now → 作品 → 最近写作 → 联系）

**改动与原因**：按已确认方案把主页从静态拼盘改成个人站入口。删 `hero-portrait` 整块；hero 全宽保留口号与逐字动画，CTA 改为「看我做的东西 / 读最新文章」。新增 Now 一行（读 `current.md` 的可选 `headline`，缺失回退 i18n）。项目卡压成一句定义。最近 3 篇真实文章（过滤 `private`）。底部一行联系 CTA。公众号入口移到 Blog 页底部。About 头像改为 160px / 88px 圆形，`object-position: 48% 26%` + `scale(1.42)` 把侧脸拉进圆心。OG 图仍用 `cu-900.jpg`，未删文件。

**影响范围**：
- cu-site：`src/pages/[lang]/index.astro`、`src/pages/[lang]/blog/index.astro`、`src/pages/[lang]/about.astro`（样式侧）、`src/data/i18n.ts`、`src/data/now/current.md`、`src/lib/now.ts`、`src/styles/global.css`、`产品文档.md`
- CuDashboard：编辑器提示与 seed 示例补可选 `headline`（见该仓库 `开发进度.md`）

**验证**：`npm run build`（astro check 0 errors + 41 pages）。本地 preview `127.0.0.1:4322` 核对 `/zh/`、`/en/`、`/zh/about/`、`/zh/blog/`。主页无 `.hero-portrait`，文档高度约 `1603px`（此前约 2500px 量级）。About 头像计算样式 160×160、`border-radius: 50%`。

**后续 / 风险**：
- 下次 CuDashboard 同步公开状态时，若 frontmatter 没有 `headline`，首页此刻一行会回退为「最近在做、写、想什么」。需在控制台公开状态里补上该字段。
- 侧脸街景圆裁仍偏环境、脸占比有限；若觉得不像头像，换更正面的照片。
- OG 品牌图仍是独立后续项。

## 2026-08-16 15:30 · 主页重构决策（只读分析，未改代码）

**背景与原因**：用户认为主页信息堆叠、不想再放大照片，提出「删主页照片，全站只在 About 留一个圆形 avatar」，并要求结合仓库现状给出主页重构方案（另有 codex 的方案作对照）。

**本次分析结论（已与用户方向对齐，实施前以对话最终确认为准）**：

1. 主页问题诊断：
   - 主页是 About / 项目 / 服务 / 写作四块静态拼盘，零动态内容（最近文章、Now 都不在主页），缺「这个人在持续产出」的信号。
   - 三处重复：主页服务 3 卡 ≈ About 能力 3 卡 ≈ 案例页尾部 CTA；主页写作渠道块 ≈ Blog 入口 + Contact 微信卡。
   - hero CTA（读博客/关于我）没有指向站内最有分量的资产（两个已上线产品 + 案例页）。
   - hero 照片 `cu-900.jpg` 是背影夜景，只有氛围价值、无「认识人」功能，可删；About 的 `about-atmosphere-720.jpg` 是侧脸照，承担唯一人像。
2. 新主页结构（五段）：全宽 hero（保留口号+逐字动画，CTA 改为「看作品/读博客」）→ Now 一行引用 → 项目两卡减重（只留 status+标题+一句定义+案例/访问链接）→ 最近 3 篇文章（真实数据，过滤 private）→ 尾部一行联系 CTA。删除：hero 照片、服务 3 卡、写作渠道大块。
3. About 圆 avatar：约 160px（桌面）/ 88px（移动）圆形。注意该照片为侧脸+街景，脸部占比小，需精调 `object-position`（脸约在水平 48% / 垂直 28% 处）并放大裁切；若效果不佳应换更正面的照片。
4. 关键架构约束（影响实施）：
   - `BaseLayout.astro` L23 的 OG 图硬编码 `cu-900.jpg`：从主页删照片不影响 OG；短期保留该 OG 图，不阻塞改版，换品牌图是独立后续项。
   - Now 一行引用：`current.md` 由 CuDashboard `lib/website-sync.ts` 自动同步，frontmatter 目前仅 `season`。方案：`src/lib/now.ts` 支持可选 `headline` 字段（缺失时回退为只显示 season+固定引导语），CuDashboard 公开状态生成处补该字段（跨仓库改动，可后做）。
   - 所有新文案须在 `i18n.ts` 双语（en/zh）同步。
   - 公众号二维码入口从主页移走，候选归属：Blog 页底部（订阅语义）或 Contact 页（已有微信卡先例），实施时二选一并做轻量化。

**影响范围/涉及文件（预计实施时）**：`src/pages/[lang]/index.astro`、`src/data/i18n.ts`、`src/styles/global.css`（`.hero*`、`.about-avatar`、新增最近文章/Now 行样式）、`src/pages/[lang]/about.astro`、`src/lib/now.ts`；可选：CuDashboard `lib/website-sync.ts` 上游生成逻辑、Blog/Contact 页（公众号入口落位）。

**已执行验证**：仅只读分析（读全部页面/样式/数据流 + 双仓库同步链路），未改代码故无构建验证。

**后续事项**：
- [ ] 用户确认方案后实施主页重构 + About 圆 avatar。
- [ ] 公众号入口落位（Blog 底部 or Contact）。
- [ ] （可选）CuDashboard 同步链路补 `headline` 字段。
- [ ] （可选）OG 图换成无人物品牌图。
- [ ] 本次补建了 `产品文档.md`（逆向整理），后续改版落地后更新其中主页描述。
