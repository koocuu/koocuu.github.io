# 产品文档 · cu-site（koocuu.com）

> 2026-08-16 由 agent 基于现有代码逆向补建，此前无此文档。产品方向或范围变化时须同步更新。

## 要解决的问题

Cu 需要一个长期维护的个人站点，作为对外的「个人入口」：让陌生访客快速认识他是谁、做过什么、在想什么，并沉淀博客、项目案例、生活足迹等长期内容，替代散落在聊天记录和各平台的碎片输出。

## 目标用户

- 潜在合作方 / 雇主：判断能力与产出（项目案例、工作经历）。
- 读者 / 同行：长期关注文章与公众号内容。
- 朋友与站主本人：足迹、此刻（经主页进入）等生活记录。

## 技术形态

- Astro 静态站点，双语（`/en/`、`/zh/`），文案集中在 `src/data/i18n.ts`（en/zh 两份，改文案必须双语同步）。
- 根路径 `/` 按浏览器语言跳转；根级 `about.astro` 等旧路径 301 到 `/en/*`。
- 部署：GitHub Pages（`.github/workflows/pages.yml`），域名 koocuu.com。
- 样式集中在 `src/styles/global.css`；标题字体霞鹜文楷 + Fraunces。

## 页面职责（2026-08 主页重构决策后）

| 页面 | 唯一职责 |
|---|---|
| 主页 `/[lang]/` | 观点 hero + 右侧 Now 一行（链到 `/now/`）→ 精选作品 → 最近 3 篇 → 联系 CTA。无人像、无独立 logo。 |
| 此刻 `/now/` | 最近在做 / 在写 / 在想（由 CuDashboard 控制台同步）。**不在一级导航**，从主页此刻一行进入。 |
| 足迹 `/travel/` | 生活经历与时间地图；标题右侧放国家/城市/最早记录概览 |
| 关于 `/about/` | 身份、能力、工作经历；全站唯一人像（圆形 avatar） |
| 博客 `/blog/` | 长期文章沉淀 |
| 联系 `/contact/` | 联系方式、合作入口与公众号 |
| 项目案例 `/projects/*` | 单个产品的完整复盘（问题→产品→取舍→结果） |

## 关键数据流

- Now 内容：CuDashboard `lib/website-sync.ts` 通过 GitHub Contents API 写入本仓库 `src/data/now/current.md`（frontmatter + `##` 分节），cu-site 侧由 `src/lib/now.ts` 解析。frontmatter 必填 `season`，可选 `headline`（首页此刻一行；缺失则用 i18n 回退文案）。解析器对其余字段向后兼容。
- 博客：`src/content/blog/*.md`，frontmatter 含 `private` 标记（列表页过滤）。
- 足迹：`src/data/travel.json`，配套 `scripts/sync-travel-data.mjs`、`geocode.mjs`。
- OG 分享图：`BaseLayout.astro` 硬编码 `/images/cu-900.jpg`（全站共用）。

## 非目标

- 不做评论区、订阅邮件等社区功能。
- 不做 CMS 后台（内容由仓库 + CuDashboard 同步链路管理）。
- 公众号内容不搬运到站内，入口只放在 Contact 页（个人微信与两个公众号）。
- 全站唯一人像在 About（圆形 avatar）；主页不放照片。OG 分享图仍用 `/images/cu-900.jpg`，与页面人像解耦。

## 验收标准（长期）

- 双语页面内容一致、互切正常。
- 移动端（≤640px）各页布局不破。
- Now 同步链路写入的文件解析失败时页面有 fallback、构建不报错。
- 主页能在一屏内传达「他是谁 + 他做了什么」，且包含会随时间更新的内容（最近文章 / Now）。
