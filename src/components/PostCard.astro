---
interface Props {
  href: string;
  title: string;
  date: Date;
  description?: string;
  tags?: string[];
  /** view-transition-name shared with the article page h1, so the title morphs across pages. */
  transitionName?: string;
}

const { href, title, date, description, tags = [], transitionName } = Astro.props;
const formattedDate = new Intl.DateTimeFormat('zh-CN', {
  year: 'numeric',
  month: '2-digit',
  day: '2-digit',
}).format(date);
---

<article class="post-card">
  <a href={href} data-astro-prefetch="hover">
    <time>{formattedDate}</time>
    <h3 transition:name={transitionName}>{title}</h3>
    {description && <p>{description}</p>}
    {tags.length > 0 && (
      <div class="tag-row">
        {tags.slice(0, 4).map((tag) => <span>{tag}</span>)}
      </div>
    )}
  </a>
</article>
